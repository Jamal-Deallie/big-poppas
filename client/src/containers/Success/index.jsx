import { useEffect } from 'react';
import { Typography, Box } from '@mui/material';

export default function SuccessContainer() {
  return (
    <Box sx={{ height: '100vh' }}>
      <Typography variant='header1'>Success Container</Typography>
    </Box>
  );
}
